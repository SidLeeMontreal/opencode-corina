{
  "name": "opencode-corina",
  "version": "0.1.0",
  "description": "Corina writing agent for OpenCode \u2014 gated 5-step editorial pipeline",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "preinstall": "node scripts/ensure-opencode-deps.mjs",
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run tests/unit",
    "test:unit": "vitest run tests/unit",
    "test:integration": "vitest run tests/integration",
    "test:regression": "vitest run tests/regression",
    "test:all": "vitest run",
    "eval": "node scripts/run-eval.mjs",
    "coverage": "vitest run --coverage tests/unit",
    "eval:tier1": "node scripts/run-eval.mjs --mode auto --suite ./evals/suites/all.json",
    "eval:tier2": "node scripts/run-eval.mjs --mode judge --suite ./evals/suites/all.json --format markdown",
    "eval:baseline": "node scripts/run-eval.mjs --mode judge --suite ./evals/suites/all.json --save-baseline",
    "install-corina": "node scripts/install-corina.mjs",
    "deploy:build": "docker build -f deploy/openwork-server/Dockerfile -t opencode-corina-openwork .",
    "deploy:run": "docker run --rm -it -p 8443:8443 -p 3001:3001 --env-file deploy/openwork-server/.env opencode-corina-openwork",
    "deploy:dev": "docker run --rm -it -p 8443:8443 -p 3001:3001 -v \"$PWD:/workspace:ro\" --env-file deploy/openwork-server/.env opencode-corina-openwork",
    "deploy:compose": "docker compose -f deploy/openwork-server/docker-compose.yml up --build"
  },
  "dependencies": {
    "@opencode-ai/plugin": "^1.4.1",
    "@opencode-ai/sdk": "^1.4.1",
    "ajv": "^8.17.1",
    "opencode-model-resolver": "file:deps/opencode-model-resolver",
    "opencode-text-tools": "file:deps/opencode-text-tools"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@vitest/coverage-v8": "^4.1.4",
    "typescript": "^5.8.3",
    "vitest": "^4.1.4",
    "opencode-eval-harness": "file:deps/opencode-eval-harness"
  },
  "keywords": [
    "opencode",
    "plugin",
    "writing",
    "corina"
  ],
  "license": "MIT"
}
